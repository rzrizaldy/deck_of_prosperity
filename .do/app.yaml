name: deck-of-capitalist
region: nyc
domains:
  - domain: deck-of-capitalist.allrize.ai
    type: PRIMARY
static_sites:
  - name: game
    git:
      repo_clone_url: https://github.com/rzrizaldy/deck_of_capitalist.git
      branch: main
    build_command: npm ci && npm run build
    output_dir: dist
    environment_slug: node-js
    catchall_document: index.html
    routes:
      - path: /

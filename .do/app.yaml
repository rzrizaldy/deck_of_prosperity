name: deck-of-capitalist
region: nyc
static_sites:
  - name: game
    github:
      repo: rzrizaldy/deck_of_capitalist
      branch: main
      deploy_on_push: true
    build_command: npm ci && npm run build
    output_dir: dist
    environment_slug: node-js
    catchall_document: index.html
    routes:
      - path: /
